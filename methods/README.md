# Shared methods

Reusable methods spanning multiple IF-R objects belong here. Study-specific protocols remain inside the relevant study directory.

Expected areas: common protocols, codebooks, instruments, templates, reproducibility tooling, and public-safe derivation methods.

A shared method must record which research objects use it and must not silently change underneath a frozen prospective protocol.
