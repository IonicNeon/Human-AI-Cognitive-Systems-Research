# Status taxonomy

Research status describes maturity, not importance.

`candidate`, `design`, `prespecified`, `collecting`, `adjudicating`, `analyzing`, `internal-review`, `preprint`, `peer-review`, `published`, `conditional`, `ethics-gated-design`, `superseded`, `withdrawn`, `abandoned`.

Compound statuses may be used when necessary but should remain machine-readable.
